{
  "name": "galeria-live",
  "version": "0.1.0",
  "private": true,
  "description": "Aplicacion web responsive para un canal de YouTube con programas, vivos, shorts, creadores y sponsors.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/config.js && node --check src/staticHandler.js && node --check public/assets/js/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
